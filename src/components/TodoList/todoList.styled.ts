import styled from "styled-components";

export const StyledList = styled.ul`
    display: flex;
    flex-direction: column;
    gap: 5px;

    margin: 0;
    padding: 0;
    margin-top: 5px;
    padding-left: 10px;

    list-style: none;
`;
